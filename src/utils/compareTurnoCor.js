


// const compareTurnoCor = async (element, array) => {
//   return (
//     element.length === element.length && // same length and
//     element.every(
//       // every element in a
//       (e1) =>
//       array.some(
//           // has a match in b
//           (e2) => e1.horas === e2.horas && e1.color === e2.color
//         )
//     )
//   );
// }

// module.exports = compareTurnoCor;